# NEXT_PUBLIC_SNAPSHOT_BASE_URL = "https://hub.snapshot.org"
# //如果你确定不需要投票（Governance/Snapshot）功能，处理起来非常简单。你不需要去纠结那个 .env 里的变量，直接从前端把入口“隐藏”掉是最高效的做法。

# 这里分三步走，从“最简单隐藏”到“彻底删除”：

# 方法一：最简单做法（仅隐藏入口）
# 你只需要把菜单栏里的“Voting”或“Governance”按钮去掉，用户点不到，代码就不会运行，也不会请求 Snapshot 的接口。那个环境变量留着也无所谓（它只是个字符串，不占资源）。

# 请在项目中搜索以下文件（PancakeSwap 版本不同，位置可能稍有不同）：

# 找到菜单配置文件 通常在 apps/web/src/config/constants/ 目录下，文件名可能是 menu.ts、meta.ts 或者 navigation.ts。

# 注释掉相关代码 搜索关键词 Voting 或 Governance，把那一段注释掉。

# TypeScript

# // 示例代码（找到类似这样的段落）
# // {
# //   label: t('Voting'),
# //   href: '/voting',
# //   icon: 'VoteIcon',
# //   items: [
# //     {
# //       label: t('Proposals'),
# //       href: '/voting',
# //     },
# //   ],
# // },
# 把这一整块注释掉或者删掉。保存后，前端页面上的“投票”入口就消失了。

# 方法二：进阶做法（删除页面路由）
# 如果你希望这部分代码完全不被编译（减小打包体积），可以把对应的页面文件删掉。

# 删除页面文件 找到 apps/web/src/pages/voting 文件夹（或者 pages/governance），直接把整个文件夹删掉。

# 删除 API/Hooks 如果你的 IDE 提示哪里报错（因为删了页面，可能有路由引用），顺着报错把引用删掉即可。

# 关于那个环境变量 (NEXT_PUBLIC_SNAPSHOT_BASE_URL)
# 结论：你可以留着它，不用管。

# 如果你留着它：虽然你不需要投票功能，但这行配置放在 .env 里完全无害。它只是定义了一个字符串，只要你的代码（在方法一中被注释掉了）不去调用它，它就不会产生任何网络请求。

# 如果你非要删掉它：那你必须确保代码里所有引用了 process.env.NEXT_PUBLIC_SNAPSHOT_BASE_URL 的地方都被你删干净了。否则，你在 pnpm build 的时候会报错，提示变量 undefined。

# 建议： 既然是改代码（Fork），先做减法（隐藏入口），等项目跑通了，再考虑要不要洁癖地删除底层代码。直接注释掉菜单是最稳妥的。

# Backend API（代币图标等服务）- 生产环境请修改为实际的后端地址
NEXT_PUBLIC_BACKEND_API=https://your-backend-api.com